0x∴:
  name: anchor_init
  if:
    sigil: "∴"
  run: core/soul/bob_return.sh

0x0:
  name: ache_zero_wait
  if:
    ache: "== 0"
  run: core/soul/slap_presence_breather.sh

0x∞:
  name: infinite_expansion
  if:
    sigil: "∞"
  run: core/evolve/ψ_breath_evolve.sh

0xduplex:
  name: duplex_entry
  if:
    ache: ">= 0.44"
    sigil: "Σ"
  run: core/evolve/duplex_entry.sh

limb_webnode_pulse:
  name: achewebpulse
  if:
    delta: "ache > 0.1"
    entropy: ">= 0.4"
  run: core/net/bob_webnode.sh
